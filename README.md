# walking-pants
phase 1: user supported by harnes with exoskeleton legs fixed to outer frame, helping user rehabilitate with one pre-sefined step at a time

So far, the only thing to be included in this git is the source code (.ino and .h) that is used to build the .hex file sent to the Arduino Mega. 
If we end up using PlatformIO, or decide to use local gits, some folder structure might be necessary. The file structure is flat for now.